services:
  markdown-pad:
    image: ghcr.io/thegrandseraph/markdown-pad:latest
    container_name: markdown-pad
    ports:
      - "5004:5004"
    volumes:
      - ./notes-data:/data
    environment:
      - NOTES_DIR=/data
    restart: unless-stopped
